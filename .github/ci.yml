name : docker CI

on:
push:
branches:
-main

jobs:
buld:
runs on : ubuntu-latest

steps:
-name: checkout code 
 uses:actions/checkout@v4

-name: build docker image
 run: docker build -t docker-todo-app .
